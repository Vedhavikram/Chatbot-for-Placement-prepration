PORT=5000
NODE_ENV=development

# JWT Secret (change this to a strong random string in production)
JWT_SECRET=placementor_super_secret_jwt_key_2024

# Database: Leave DATABASE_URL empty to use SQLite (recommended for local dev)
# DATABASE_URL=postgresql://user:password@localhost:5432/placementor

# Anthropic Claude API Key (optional - falls back to smart mock responses)
# ANTHROPIC_API_KEY=sk-ant-...

# CORS Origin for frontend
FRONTEND_URL=http://localhost:5173
